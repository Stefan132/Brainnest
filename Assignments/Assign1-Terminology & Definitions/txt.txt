1. What do HTML and CSS stand for?

A: --> The HyperText Markup Language or HTML is the standard markup language for documents designed to be displayed in a web browser
   --> Cascading Style Sheets or CSS is a style sheet language used for describing the presentation of a document written in a markup language such as HTML.

2. Would you rather use HTML or CSS, for putting paragraphs of text on a webpage?

A: --> For inserting paragraphs or text on a webpage we need to use HTML language. 

3. Would you rather use HTML or CSS, to change the font and background color of abutton?

A: --> For styling of a button we need to use CSS language.

4. What is an HTML tag?

A: --> The HTML tag represents the root of an HTML document.

5. What are the three parts of an HTML element? (image from paragraph)

A: --> The three parts of an HTML element are the angle brackets,the opening and closing tags.

6. What is the purpose of the doctype declaration?

A: --> The DOCTYPE declaration is an instruction to the web browser about what version of HTML the page is written in. 

7. What is the HTML element?

A: --> HTML element represents the root of an HTML document.

8. What is the purpose of the head element?

A: --> The <head> element is a container for metadata (data about data) and is placed between the <html> tag and the <body> tag.

9. What is the purpose of the body element?

A: --> The <body> tag defines the document's body.

10. How do you create a paragraph in HTML?

A: --> This is how you create a paragraph in HTML <p>text</p>

11. How do you create a heading in HTML?

A: --> This is how you create a heading in HTML <h1>Heading</h1>

12. How many different levels of headings are there and what is the difference between them?

A: --> There are 6 difference levels of headings from <h1> to <h6>.

13. What element should you use to make text bold and important?

A: --> To make the text bold we need to use this following element <b></b>.

14. What element should you use to make text italicized to add emphasis to it?

A: --> To make the text italicized we need to use this following element <i></i>.

15. What relationship does an element have with any nested elements within it?

A: --> When we nest elements within other elements, we create a parent and child relationship between them.

16. What relationship do two elements have if they are at the same level of nesting?

A: --> They have relationship of siblings.

17. How do you create HTML comments?

A: --> This is how we create comments ! <--- Html Comments --->

18. What HTML tag is used to create an unordered list?

A: --> For creating an unordered list we use the following tag <ul></ul>.

19. What HTML tag is used to create an ordered list?

A: -->  For creating an ordered list we use the following tag <ol></ol>.

20. What HTML tag is used to create list items within both unordered and ordered lists?

A: -->  <li></li>

21. What element is used to create a link?

A: --> We use the href attribute <a href="link">

22. What is an attribute?

A: --> Attributes define additional characteristics or properties of the element such as width and height of an image.

23. What attribute tells links where to go?

A: --> href atribute.

24. What is the difference between an absolute and relative link?

A: --> The main difference between absolute and relative paths is that absolute URLs always include the domain name of the site with http://www.
     Relative links show the path to the file or refer to the file itself. 

25. Which element is used to display an image?

A: --> The element we use to display an image is <img>.

26. What two attributes do images always need to have?

A: --> src and alt atribute

27. How do you access a parent directory in a file path?

A: --> Use ./ before the directory that is in use.

28. What are the four main image formats that you can use for images on the web?

A: --> JPG, PNG, GIF and SVG